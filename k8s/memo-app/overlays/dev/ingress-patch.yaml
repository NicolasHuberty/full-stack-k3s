apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: memo-app-ingress
  namespace: memo-app-dev
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/hostname: dev.memo.docuralis.com
spec:
  tls:
  - hosts:
    - dev.memo.docuralis.com
    secretName: memo-app-dev-tls
  rules:
  - host: dev.memo.docuralis.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dev-memo-frontend
            port:
              number: 3000
